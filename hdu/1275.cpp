#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <cmath>
#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

#define FOR(i,s,t) for (int i = s; i < t; ++i)
#define MEM(s,v) memset(s, v, sizeof(s))

#define EPS 1e-8
#define _N 100005
#define _M 105
#define PI acos(-1.0)

int _, k, d1, d2;
double v1, v2, dist;
double x, t, t0;

int main()
{
	scanf("%d", &_); while (_--) {
		scanf("%lf%lf%lf%d", &dist, &v1, &v2, &k);
		t = dist / (v1 + v2);
		while (--k) {
			t0 = 
		}
	}
	return 0;
}
